const router = require('express').Router();
// grab models


// routes:

// READ ALL ROUTE (GET)

// READ SINGLE ROUTE (GET)

// CREATE ROUTE (POST)

// UPDATE ROUTE (PUT)

// DELETE ROUTE (DELETE)


module.exports = router;